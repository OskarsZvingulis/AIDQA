# Environment Variables Configuration

# ============================================
# FRONTEND CONFIGURATION (Vercel/Vite)
# ============================================
# IMPORTANT: Leave VITE_API_BASE_URL empty for local development!
# The Supabase local dev server will handle API routes automatically.
# 
# For production deployment:
# VITE_API_BASE_URL=https://your-project.supabase.co/functions/v1/visual-api
#
# Example: VITE_API_BASE_URL=https://abcdefghijklmno.supabase.co/functions/v1/visual-api
VITE_API_BASE_URL=

# ============================================
# SUPABASE EDGE FUNCTION SECRETS
# ============================================
# Set these using: supabase secrets set <KEY>=<VALUE>
# DO NOT commit these values to git!

# Supabase Configuration (Required)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_...

# Storage Bucket Name (Required, default: visual)
# STORAGE_BUCKET=visual

# Remote Browser WebSocket Endpoint (Required)
# Get from Browserless.io or similar service
# BROWSERLESS_WS_ENDPOINT=wss://chrome.browserless.io?token=YOUR_TOKEN

# CORS Configuration (Optional)
# Comma-separated list of allowed origins, or '*' for all
# ALLOWED_ORIGINS=https://aidqa.vercel.app,http://localhost:8080

# Rate Limiting (Optional, default: 30 requests per minute)
# RATE_LIMIT_PER_MINUTE=30

# AI Insights (Optional - OpenAI)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o
# AI_ENABLED=true

# SSRF Protection (Optional)
# Comma-separated list of allowed hosts (if not set, all public hosts allowed)
# ALLOWED_HOSTS=example.com,yoursite.com

# ============================================
# LEGACY/DEPRECATED (Old Express API)
# ============================================
# These are no longer needed with Supabase Edge Functions
# FIGMA_ACCESS_TOKEN=
# PORT=8787
# NODE_ENV=development


